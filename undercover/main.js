// Point d'entrée : lance le premier rendu une fois tous les scripts chargés.

/* ---------------------------------------------------------
   DÉMARRAGE
--------------------------------------------------------- */
applyTheme(loadTheme());
render();
